Apex Tracker:
  ✔ Move diff algorithm to API manager
  ✔ move the timeout from client to server
  ☐ *All in session* avg kill/game, highest kills, wins, highest dmg, # played
  ☐ make styles settings somehow so I don't save only mine to git
